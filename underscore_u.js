// ~~~> _u. utilities <~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
        _u = (function(){
            var alog = function(text,label){
                    log((label ? label + ': ' : '') + text);
                };
            return {
                alog: alog
            };
        }()),
// ~~~> _u. utilities <~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
